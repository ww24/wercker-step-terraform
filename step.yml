name: terraform
version: 0.4.2
description: Step to setup and run terraform
summary: Step to setup and run terraform
keywords:
    - terraform
properties:
  - name: command
        type: string
        required: false
        default: apply
  - name: var_file
        type: string
        required: false
  - name: remote_config
        type: string
        required: false
