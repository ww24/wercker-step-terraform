name: terraform
version: 0.2.0
description: Step to setup and run terraform
keywords:
    - terraform
properties:
    command:
        type: string
        required: false
        default: apply
    var_file:
        type: string
        required: false
    remote_config:
        type: string
        required: false
